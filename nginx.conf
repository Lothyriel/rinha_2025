events {}

http {
    upstream api {
        server uds:/var/run/api_0.sock;
        server uds:/var/run/api_1.sock;
    }

    server {
        listen 80;

        location / {
            proxy_pass http://api;
        }
    }
}
