x-api_template: &api
  image: localhost/rinha
  volumes:
    - uds:/var/run
  command: ["-m", "api"]
  init: true
  depends_on:
    - worker
  environment:
    RUST_LOG: "info"
  deploy:
    resources:
      limits:
        cpus: "0.4"
        memory: "80MB"

services:
  lb:
    image: nginx:latest
    ports:
      - "9999:9999"
    volumes:
      - uds:/var/run
      - ./nginx.conf:/etc/nginx/nginx.conf:ro
    depends_on:
      - api0
      - api1
    deploy:
      resources:
        limits:
          cpus: "0.4"
          memory: "80MB"

  api0:
    <<: *api
    environment:
      API_N: "0"
      RUST_LOG: "info"
      WORKER_SOCKET: "/var/run/worker.socket"

  api1:
    <<: *api
    environment:
      API_N: "1"
      RUST_LOG: "info"
      WORKER_SOCKET: "/var/run/worker.socket"

  worker:
    image: localhost/rinha
    command: ["-m", "worker"]
    init: true
    networks:
      - payment-processor
    volumes:
      - uds:/var/run
    environment:
      WORKER_SOCKET: "/var/run/worker.socket"
      RUST_LOG: "info"
    deploy:
      resources:
        limits:
          cpus: "0.3"
          memory: "110MB"

volumes:
  uds:

networks:
  payment-processor:
    external: true
